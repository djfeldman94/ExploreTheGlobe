<?xml version="1.0" encoding="utf-8"?>
<!-- New layout file, with enhanced support for accessibility -->
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:minHeight="@dimen/min_target_height"
    android:minWidth="@dimen/min_target_width"
    android:background="?attr/selectableItemBackground"
    android:focusable="true"
    android:clickable="true"
    android:importantForAccessibility="yes"
    android:layout_margin="8dp"
    app:cardCornerRadius="8dp"
    app:cardElevation="4dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="@dimen/medium_padding">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/nameRegionText"
                android:accessibilityHeading="true"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:maxLines="1"
                android:layout_weight="1"
                android:textSize="@dimen/text_size_medium"
                android:textStyle="bold"
                tools:text="Afghanistan, AS"/>



            <TextView
                android:id="@+id/codeText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:maxLines="1"
                android:layout_marginStart="@dimen/small_padding"
                android:textSize="@dimen/text_size_medium"
                android:textStyle="bold"
                tools:text="AF"/>

        </LinearLayout>
        <TextView
            android:id="@+id/capitalText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/small_padding"
            android:textSize="@dimen/text_size_small"
            tools:text="Kabul"/>

    </LinearLayout>

</androidx.cardview.widget.CardView>